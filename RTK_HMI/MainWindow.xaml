<Window x:Class="RTK_HMI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RTK_HMI"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        xmlns:fa="clr-namespace:FontAwesome.Sharp;assembly=FontAwesome.Sharp"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:m="clr-namespace:DataAccess.Models;assembly=DataAccess"
        mc:Ignorable="d"
        xmlns:uc="clr-namespace:RTK_HMI.Views.UserControls"
        xmlns:vm="clr-namespace:RTK_HMI.ViewModels"
        Title="RTKCOMM" WindowState="Maximized"
        Background="#262626">
    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>
    <Grid>
        <TabControl>
            <TabItem Header="Main page">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="50"/>
                        <RowDefinition/>
                    </Grid.RowDefinitions>
                    <uc:ConnectPanel DataContext="{Binding ConnectVM}"/>
                    <uc:ReadWriteParametersControl Grid.Row="1"/>
                    
                    <fa:IconBlock Grid.Row="1"
                                     Visibility="{Binding ConnectVM.LoadIndicator, Converter={StaticResource VisibleIfEqual}, ConverterParameter=True}"
                                     Icon="ArrowsRotate" Height="40"
                                     RenderTransformOrigin="0.5,0.5">
                        <fa:IconBlock.RenderTransform>
                            <RotateTransform/>
                        </fa:IconBlock.RenderTransform>
                        <fa:IconBlock.Triggers>
                            <EventTrigger RoutedEvent="Loaded">
                                <BeginStoryboard>
                                    <Storyboard RepeatBehavior="Forever">
                                        <DoubleAnimation
                                            Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)"
                                            From="0" To="360" Duration="0:0:1"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </fa:IconBlock.Triggers>
                    </fa:IconBlock>
                </Grid>
            </TabItem>
            <TabItem Header="Settings" Visibility="{Binding UserVm.CurrentUser, Converter={StaticResource UserToVisibilityConverter}, ConverterParameter=Admin, TargetNullValue={x:Static Visibility.Collapsed}}">
                <uc:UsersControl DataContext="{Binding UserVm}"/>
            </TabItem>
            <TabItem Header="PLC" Visibility="{Binding UserVm.IsAuthorized, Converter={StaticResource VisibleIfEqual}, ConverterParameter=True}">
                <Grid ColumnDefinitions="*,*" DataContext="{Binding PlcVm}">
                    <ListBox>
                        <DockPanel>
                            <TextBlock Foreground="White" Text="{Binding IsPlcConnected, StringFormat='PLC connect status: {0}'}" Margin="20, 5" FontSize="24"/>
                            <Button Command="{Binding ConnectPlcCommand}" Content="ПОДКЛЮЧИТЬСЯ" Foreground="Wheat"/>
                            <Button Command="{Binding DisconnectPlcCommand}" Content="ОТКЛЮЧИТЬСЯ" Foreground="Wheat"/>
                        </DockPanel>
                        
                        <TextBlock Foreground="White" Text="{Binding plcDevice.Mode, StringFormat='PLC Mode: {0}'}" Margin="20, 5"/>
                        <DockPanel Margin="20, 5" >
                            <TextBlock Text="Plc Run status" Foreground="White"/>
                            <CheckBox IsChecked="{Binding plcDevice.Run}"/>
                        </DockPanel>
                        <DockPanel Margin="20, 5">
                            <TextBlock Text="Plc RstErrors" Foreground="White"/>
                            <CheckBox IsChecked="{Binding plcDevice.RstErrors}"/>
                        </DockPanel>
                        <TextBlock Foreground="White" Text="{Binding plcDevice.HummSensorView.Adc, StringFormat='HummSensor View Adc {0}'}" Margin="20, 5"/>
                        <TextBlock Foreground="White" Text="{Binding plcDevice.HummSensorView.Value, StringFormat='HummSensor Value {0}'}" Margin="20, 5"/>
                        <TextBlock Foreground="White" Text="{Binding plcDevice.HummSensorView.VoltageCur, StringFormat='HummSensor VoltageCur {0}'}" Margin="20, 5"/>

                        <TextBlock Foreground="White" Text="{Binding plcDevice.TempSensorView.Adc, StringFormat='TempSensor View Adc {0}'}" Margin="20, 5"/>
                        <TextBlock Foreground="White" Text="{Binding plcDevice.TempSensorView.Value, StringFormat='TempSensor Value {0}'}" Margin="20, 5"/>
                        <TextBlock Foreground="White" Text="{Binding plcDevice.TempSensorView.VoltageCur, StringFormat='TempSensor VoltageCur {0}'}" Margin="20, 5"/>

                        <TextBlock Foreground="White" Text="{Binding plcDevice.DiffPressureView.Adc, StringFormat='DiffPressure View Adc {0}'}" Margin="20, 5"/>
                        <TextBlock Foreground="White" Text="{Binding plcDevice.DiffPressureView.Value, StringFormat='DiffPressure Value {0}'}" Margin="20, 5"/>
                        <TextBlock Foreground="White" Text="{Binding plcDevice.DiffPressureView.VoltageCur, StringFormat='DiffPressure VoltageCur {0}'}" Margin="20, 5"/>

                        <TextBlock Foreground="White" Text="{Binding plcDevice.AbsPressureView.Adc, StringFormat='AbsPressure View Adc {0}'}" Margin="20, 5"/>
                        <TextBlock Foreground="White" Text="{Binding plcDevice.AbsPressureView.Value, StringFormat='AbsPressure Value {0}'}" Margin="20, 5"/>
                        <TextBlock Foreground="White" Text="{Binding plcDevice.AbsPressureView.VoltageCur, StringFormat='AbsPressure VoltageCur {0}'}" Margin="20, 5"/>

                        <TextBlock Foreground="White" Text="{Binding plcDevice.FlowView.Density, StringFormat='FlowView Density {0}'}" Margin="20, 5"/>
                        <TextBlock Foreground="White" Text="{Binding plcDevice.FlowView.Velocity, StringFormat='FlowView Velocity {0}'}" Margin="20, 5"/>
                        <TextBlock Foreground="White" Text="{Binding plcDevice.FlowView.DeltaPFilter, StringFormat='FlowView DeltaPFilter {0}'}" Margin="20, 5"/>
                        <TextBlock Foreground="White" Text="{Binding plcDevice.FlowView.DeltaPNet, StringFormat='FlowView DeltaPNet {0}'}" Margin="20, 5"/>

                        <TextBlock Foreground="White" Text="{Binding plcDevice.PidView.Pv, StringFormat='PID View Pv {0}'}" Margin="20, 5"/>
                        <TextBlock Foreground="White" Text="{Binding plcDevice.PidView.StatePrev, StringFormat='PID View StatePrev {0}'}" Margin="20, 5"/>
                        <TextBlock Foreground="White" Text="{Binding plcDevice.PidView.State, StringFormat='PID View State {0}'}" Margin="20, 5"/>
                        <TextBlock Foreground="White" Text="{Binding plcDevice.PidView.OutHz, StringFormat='PID View OutHz {0}'}" Margin="20, 5"/>
                        <TextBlock Foreground="White" Text="{Binding plcDevice.PidView.OutLast, StringFormat='PID View OutLast {0}'}" Margin="20, 5"/>
                        <TextBlock Foreground="White" Text="{Binding plcDevice.PidView.OutPct, StringFormat='PID View OutPct {0}'}" Margin="20, 5"/>
                        <TextBlock Foreground="White" Text="{Binding plcDevice.PidView.Tuned, StringFormat='PID View Tuned {0}'}" Margin="20, 5"/>
                        <TextBlock Foreground="White" Text="{Binding plcDevice.PidView.ErrorBits, StringFormat='PID View ErrorBits {0}'}" Margin="20, 5"/>
                        <TextBlock Foreground="White" Text="{Binding plcDevice.PidView.InRangeTmr, StringFormat='PID View InRangeTmr {0}'}" Margin="20, 5"/>
                    </ListBox>
                    <ListBox Grid.Column="1">
                        <TextBlock Foreground="White" Text="{Binding plcDevice.HummSensorSettings.InputType, StringFormat='HummSensor Input Type {0}'}" Margin="20, 5"/>
                        <TextBlock Foreground="White" Text="{Binding plcDevice.HummSensorSettings.MaxAdc, StringFormat='HummSensor MaxAdc {0}'}" Margin="20, 5"/>
                        <TextBlock Foreground="White" Text="{Binding plcDevice.HummSensorSettings.MinAdc, StringFormat='HummSensor MinAdc {0}'}" Margin="20, 5"/>
                        <TextBlock Foreground="White" Text="{Binding plcDevice.HummSensorSettings.MaxValue, StringFormat='HummSensor MaxValue {0}'}" Margin="20, 5"/>
                        <TextBlock Foreground="White" Text="{Binding plcDevice.HummSensorSettings.MinValue, StringFormat='HummSensor MinValue {0}'}" Margin="20, 5"/>
                        
                        <TextBlock Foreground="White" Text="{Binding plcDevice.TempSensorSettings.InputType, StringFormat='TempSensor Input Type {0}'}" Margin="20, 5"/>
                        <TextBlock Foreground="White" Text="{Binding plcDevice.TempSensorSettings.MaxAdc, StringFormat='TempSensor MaxAdc {0}'}" Margin="20, 5"/>
                        <TextBlock Foreground="White" Text="{Binding plcDevice.TempSensorSettings.MinAdc, StringFormat='TempSensor MinAdc {0}'}" Margin="20, 5"/>
                        <TextBlock Foreground="White" Text="{Binding plcDevice.TempSensorSettings.MaxValue, StringFormat='TempSensor MaxValue {0}'}" Margin="20, 5"/>
                        <TextBlock Foreground="White" Text="{Binding plcDevice.TempSensorSettings.MinValue, StringFormat='TempSensor MinValue {0}'}" Margin="20, 5"/>

                        <TextBlock Foreground="White" Text="{Binding plcDevice.DiffPressureSettings.InputType, StringFormat='DiffPressure Input Type {0}'}" Margin="20, 5"/>
                        <TextBlock Foreground="White" Text="{Binding plcDevice.DiffPressureSettings.MaxAdc, StringFormat='DiffPressure MaxAdc {0}'}" Margin="20, 5"/>
                        <TextBlock Foreground="White" Text="{Binding plcDevice.DiffPressureSettings.MinAdc, StringFormat='DiffPressure MinAdc {0}'}" Margin="20, 5"/>
                        <TextBlock Foreground="White" Text="{Binding plcDevice.DiffPressureSettings.MaxValue, StringFormat='DiffPressure MaxValue {0}'}" Margin="20, 5"/>
                        <TextBlock Foreground="White" Text="{Binding plcDevice.DiffPressureSettings.MinValue, StringFormat='DiffPressure MinValue {0}'}" Margin="20, 5"/>

                        <TextBlock Foreground="White" Text="{Binding plcDevice.AbsPressureSettings.InputType, StringFormat='AbsPressure Input Type {0}'}" Margin="20, 5"/>
                        <TextBlock Foreground="White" Text="{Binding plcDevice.AbsPressureSettings.MaxAdc, StringFormat='AbsPressure MaxAdc {0}'}" Margin="20, 5"/>
                        <TextBlock Foreground="White" Text="{Binding plcDevice.AbsPressureSettings.MinAdc, StringFormat='AbsPressure MinAdc {0}'}" Margin="20, 5"/>
                        <TextBlock Foreground="White" Text="{Binding plcDevice.AbsPressureSettings.MaxValue, StringFormat='AbsPressure MaxValue {0}'}" Margin="20, 5"/>
                        <TextBlock Foreground="White" Text="{Binding plcDevice.AbsPressureSettings.MinValue, StringFormat='AbsPressure MinValue {0}'}" Margin="20, 5"/>

                        <TextBlock Foreground="White" Text="{Binding plcDevice.FlowSettings.GasType, StringFormat='FlowSettings GasType {0}'}" Margin="20, 5"/>
                        <TextBlock Foreground="White" Text="{Binding plcDevice.FlowSettings.KProbe, StringFormat='FlowSettings KProbe {0}'}" Margin="20, 5"/>
                        <TextBlock Foreground="White" Text="{Binding plcDevice.FlowSettings.DeltaPCutOff, StringFormat='FlowSettings DeltaPCutOff {0}'}" Margin="20, 5"/>
                        <TextBlock Foreground="White" Text="{Binding plcDevice.FlowSettings.DeltaPHyst, StringFormat='FlowSettings DeltaPHyst {0}'}" Margin="20, 5"/>
                        <TextBlock Foreground="White" Text="{Binding plcDevice.FlowSettings.DeltaPZero, StringFormat='FlowSettings DeltaPZero {0}'}" Margin="20, 5"/>
                        <TextBlock Foreground="White" Text="{Binding plcDevice.FlowSettings.FilterTimeConst, StringFormat='FlowSettings FilterTimeConst {0}'}" Margin="20, 5"/>

                        <TextBlock Foreground="White" Text="{Binding plcDevice.PidSettings.Speed, StringFormat='PidSettings Speed {0}'}" Margin="20, 5"/>
                        <TextBlock Foreground="White" Text="{Binding plcDevice.PidSettings.ManPct, StringFormat='PidSettings ManPct {0}'}" Margin="20, 5"/>
                        <TextBlock Foreground="White" Text="{Binding plcDevice.PidSettings.Tol, StringFormat='PidSettings Tol {0}'}" Margin="20, 5"/>
                        <TextBlock Foreground="White" Text="{Binding plcDevice.PidSettings.InRangeTime, StringFormat='PidSettings InRangeTime {0}'}" Margin="20, 5"/>
                    </ListBox>
                </Grid>

            </TabItem>
            <TabItem Header="Calibration" Visibility="{Binding UserVm.IsAuthorized, Converter={StaticResource VisibleIfEqual}, ConverterParameter=True}">
                <uc:CalibrationControl DataContext="{Binding CalibrationVm}"/>
            </TabItem>
            <TabItem Header="Logging" Visibility="{Binding UserVm.IsAuthorized, Converter={StaticResource VisibleIfEqual}, ConverterParameter=True}">
                <uc:LoggingControl DataContext="{Binding LoggingVm}"/>
            </TabItem>
        </TabControl>
        <StackPanel Orientation="Horizontal" VerticalAlignment="Top" HorizontalAlignment="Right" DataContext="{Binding UserVm}" Margin="10,0,10,0">
            <!--#region Login -->
            <Button BorderThickness="0" Background="Transparent" Command="{Binding AuthCommand}">
                <Image     Height="32" Width="32"                       
                       Source="{Binding IsAuthorized, Converter={StaticResource GetIfTrueConverter}, ConverterParameter={StaticResource UserIcons}}" 
                       >
                </Image>
            </Button>
            <!--#endregion-->
            <!--#region Name -->
            <TextBlock  FontSize="16"
                        VerticalAlignment="Center"
                        FontWeight="Regular"
                        Text="{Binding CurrentUser, Converter={StaticResource UserTextConverter}}"
                        Foreground="{Binding IsAuthorized, Converter={StaticResource GetIfTrueConverter}, ConverterParameter={StaticResource UserBackground }}"/>
            <!--#endregion-->
            <!--#region LogOut -->
            <!--#region Login -->
            <Button BorderThickness="0" Background="Transparent" Command="{Binding LogoutCommand}" Visibility="{Binding IsAuthorized, Converter={StaticResource VisibleIfEqual}, ConverterParameter=True}">
                <Image Height="32" Width="32"                       
                       Source="Views\Resourses\Pictures\ResetTv.png" 
                       >
                </Image>
            </Button>
            <!--#endregion-->
            <!--#endregion-->
        </StackPanel>

        <TextBlock FontSize="14" VerticalAlignment="Bottom" Text="{Binding Version, StringFormat=VISSMA FLOW Version:{0}}" Style="{StaticResource TextBlockCommonStyle}" HorizontalAlignment="Right" Margin="10"/>
    </Grid>
    
</Window>
