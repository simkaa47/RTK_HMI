<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:fa="clr-namespace:FontAwesome.Sharp;assembly=FontAwesome.Sharp">
    <Style  TargetType="Button">
        <Setter Property="Background" Value="#323232"/>
        <Setter Property="Foreground" Value="#323232"/>
        <Setter Property="Margin" Value="5"/>
    </Style>
    <Style TargetType="fa:IconBlock">
        <Setter Property="Foreground" Value="#ADADAD"/>
        <Setter Property="FontSize" Value="22"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>
    <!--<Style x:Key="SaveButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
        <Setter Property="Background" Value="Transparent"/>

        <Style.Triggers>
            <DataTrigger Binding="{Binding IsLogging}" Value="True">
                <Setter Property="Content" Value="Stop logging"/>
                <Setter Property="Background" Value="Red"/>
                <Setter Property="Foreground" Value="White"/>
            </DataTrigger>
            <DataTrigger Binding="{Binding IsLogging}" Value="False">
                <Setter Property="Content" Value="Start logging"/>
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="Foreground" Value="Gray"/>
            </DataTrigger>
            --><!-- MouseOver для состояния когда IsLogging = False --><!--
            <MultiDataTrigger>
                <MultiDataTrigger.Conditions>
                    <Condition Binding="{Binding IsLogging}" Value="False"/>
                    <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=IsMouseOver}" Value="True"/>
                </MultiDataTrigger.Conditions>
                <Setter Property="Background" Value="#333333"/>
            </MultiDataTrigger>

            --><!-- MouseOver для состояния когда IsLogging = True --><!--
            <MultiDataTrigger>
                <MultiDataTrigger.Conditions>
                    <Condition Binding="{Binding IsLogging}" Value="True"/>
                    <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=IsMouseOver}" Value="True"/>
                </MultiDataTrigger.Conditions>
                <Setter Property="Background" Value="#CC0000"/>
            </MultiDataTrigger>--><!--
        </Style.Triggers>
    </Style>-->
    
</ResourceDictionary>